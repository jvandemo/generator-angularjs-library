language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-angularjs-library' ]; then cd .. && eval "mv $currentfolder generator-angularjs-library" && cd generator-angularjs-library; fi

